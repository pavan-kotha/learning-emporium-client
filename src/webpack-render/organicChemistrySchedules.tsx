import * as React from "react";
import OrganicChemistrySchedules from "../pages/OrganicChemistrySchedules";
import { createRoot } from "react-dom/client";

const root = document.getElementById("root");

createRoot(root).render(<OrganicChemistrySchedules />);
