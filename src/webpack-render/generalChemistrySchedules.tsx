import * as React from "react";
import GeneralChemistrySchedules from "../pages/GeneralChemistrySchedules";
import { createRoot } from "react-dom/client";

const root = document.getElementById("root");

createRoot(root).render(<GeneralChemistrySchedules />);
