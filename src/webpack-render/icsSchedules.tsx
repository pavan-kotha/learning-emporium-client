import * as React from "react";
import IcsSchedules from "../pages/IcsSchedules";
import { createRoot } from "react-dom/client";

const root = document.getElementById("root");

createRoot(root).render(<IcsSchedules />);
